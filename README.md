This is an example macro project to learn basic SOLIDWORKS API operations. It focuses on changing dimensions of parts within an assembly, then adding a UI.
The VBA macro file is changeDims.swp.
The additional files are exported modules, classes and forms for the macro.
